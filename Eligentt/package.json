{
  "name": "elligentt",
  "version": "5.0.0",
  "private": true,
  "description": "Elligentt — Batch Payments, Swap & Bridge on Arc Testnet",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "vite",
    "sync": "node scripts/verify-shared-sync.cjs",
    "deploy": "node scripts/build.js && npx wrangler pages deploy public/"
  },
  "devDependencies": {
    "terser": "^5.49.2",
    "vite": "^6.0.0",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "ethers": "^6.13.5",
    "pdfkit": "^0.19.1"
  }
}
